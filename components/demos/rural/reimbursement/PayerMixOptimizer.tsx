"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/demos/rural/shared/ui/Card";

export function PayerMixOptimizer() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Payer mix optimizer</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-arka-text-dark-muted">
        <p>Model shifts between Medicare, Medicaid, and commercial share — demo visualization placeholder.</p>
      </CardContent>
    </Card>
  );
}
