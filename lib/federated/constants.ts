/** Maximum epsilon spend per CPT code per rolling 7-day window. */
export const MAX_EPSILON_PER_CPT_PER_WEEK = 5;

/** Minimum institutions required for a federated mean query in production. */
export const MIN_FEDERATED_INSTITUTIONS = 2;
